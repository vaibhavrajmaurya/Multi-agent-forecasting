# `docs/` — Documentation

| Item | What it is |
|---|---|
| `REPORT.html` | **Complete technical report.** Every term defined, every module explained line by line, each agent end-to-end, all 12 corrections from the baseline, and the final results with honest interpretation. Open in a browser and use **Save as PDF** (or Ctrl/Cmd+P) for a PDF. |
| `CORRECTIONS.md` | **Engineering & methodology log.** Every fix made from the first run of the baseline onward — including the two honest negative results (a P&L-aligned loss and a conviction threshold that did not help). |
| `resources/` | Supporting PDFs (course introduction and weekly reading material). |

Start with `REPORT.html` for the full picture, or `CORRECTIONS.md` for the debugging and
methodology trail.
